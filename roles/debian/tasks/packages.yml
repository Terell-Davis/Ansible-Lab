---
- name: Install the basics
  ansible.builtin.package:
    name:
      - sudo
      - net-tools
      - curl
      - ufw
      - htop
      - samba
      - unzip
      - zip
    state: latest
  become: true
